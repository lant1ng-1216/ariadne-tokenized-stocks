# Ariadne API 配置

请直接打开同一文件夹里的 `.env` 文件，并填写前两行：

```env
BINANCE_WEB3_API_KEY=你的API密钥
BINANCE_WEB3_API_SECRET=你的私钥
BINANCE_WEB3_BASE_URL=https://web3.binance.com/build
```

填写完成后保存即可。`.env` 位于项目根目录，也就是和 `PRD.md`、`.env.example` 同一层级。

不要修改变量名，不要加引号，不要在等号两边加空格。
